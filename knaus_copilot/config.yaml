name: "Knaus Copilot"
version: "0.1.0"
slug: "knaus_copilot"
description: "Assistente AI personale per supervisione, memoria e diario della caravan."
url: "https://github.com/mistermif/knaus-copilot-apps"
arch:
  - aarch64
  - amd64
  - armv7
startup: application
boot: auto
init: false
ingress: true
ingress_port: 8099
panel_icon: "mdi:caravan"
panel_title: "Knaus Copilot"
homeassistant_api: true
auth_api: true
options:
  model: "gpt-5.4-mini"
  autonomy_mode: "observe"
  max_context_entities: 80
  log_level: "info"
schema:
  openai_api_key: "password?"
  model: str
  autonomy_mode: "list(observe|confirm|limited)"
  max_context_entities: "int(10,200)"
  log_level: "list(debug|info|warning|error)"
